{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. `dialog:allow-open` is the folder picker behind Add folder; nothing else in the dialog plugin is granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
